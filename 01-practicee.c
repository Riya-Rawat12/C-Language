#include <stdio.h>
int main(){
    int principal=120 ,rate=2,years=1;
    int simpleinterest= (principal* rate* years)/100;
    printf("the value of simpleinterest is %d", simpleinterest);

return 0;
}