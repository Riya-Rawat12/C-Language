#include <stdio.h>
int main(){
    int length=3;
    int breadth=8;
    int area = length*breadth;
    printf("the area of the rectangle is %d\n",area);
    scanf("%d%d",&length,&breadth);
return 0;
}