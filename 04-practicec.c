#include <stdio.h>
int main(){
    int n;
    printf("Enter the value of n\n",n);
    scanf("%D",&n);
    for(int i=n; i; i++){
        printf("Natural no. are %d\n",i+1);
        
    }
return 0;
}